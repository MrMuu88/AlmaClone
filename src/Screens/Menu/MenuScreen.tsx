import { View, Text } from 'react-native'
import React from 'react'
import { colors } from '../../Styles/StyleSheet'

export const MenuScreen = ()=> {
  return (
    <View>
      <Text style={{color:colors.primary}}>MenuScreen</Text>
    </View>
  )
}